<div class="page-wrapper">
  <div class="page-breadcrumb bg-white">
    <div class="row align-items-center">
      <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
        <h2 class="d-inline-block">Danh sách loại thiết bị</h2>
      </div>
    </div>
  </div>
  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-12">
        <div class="white-box">
          <a href="/list-catogory/add" class="btn btn-success">
            Thêm loại thiết bị
          </a>
          <div class="table-responsive">
            <table id="data" class="table text-nowrap text-center">
              <thead>
                <tr>
                  <th scope="col">Mã loại thiết bị</th>
                  <th scope="col">Tên loại thiết bị
                  </th>
                  <th scope="col">Thao tác
                  </th>
                </tr>
              </thead>
              <tbody>
                {{#each loaithietbi}}
                <tr>
                  <th scope="row">{{this.idloai}}</th>
                  <td>{{this.tenloai}}</td>
                  <td>
                    <form class="d-inline-block" action="/list-catogory/detail/{{this.idloai}}" method="get">
                      <button class="btn btn-info" type="submit" data-placement="top" title="Xem chi tiết">
                        <i class="fa fa-info-circle"></i>

                      </button>
                    </form>
                    <form id="{{this.idloai}}" class="d-inline-block"
                      action="/list-catogory/delete/{{this.idloai}}?_method=DELETE" method="POST">
                      <button type="button" class="btn btn-danger" data-toggle="tooltip" data-placement="top"
                        title="Xoá" onclick="myFunction({{this.idloai}})">
                        <i class="fa fa-trash-alt"></i>
                      </button>
                    </form>
                  </td>
                </tr>
                {{/each}}
              </tbody>
            </table>
            <ul id="nav" class="pagination justify-content-center"></ul>
            <script>
              function myFunction(id) {
                let r = confirm('Xác nhận xóa loại thiết bị');
                if (r == true) {
                  //alert("delete" + id);
                  document.getElementById(id).submit();
                  //alert(id);
                }
              }
            </script>
            <script>
                        $(document).ready(function () {
                            $('#nav').children().remove();
                            var rowsShown = 5;
                            var rowsTotal = $('#data tbody tr').length;
                            var numPages = rowsTotal / rowsShown;
                            for (i = 0; i < numPages; i++) {
                                var pageNum = i + 1;
                                $('#nav').append(
                                    '<li class="page-item"><a id="f" class="page-link" href="#" rel="' + i +
                                    '">' + pageNum + '</a> </li>');
                            }
                            $('#data tbody tr').hide();
                            $('#data tbody tr').slice(0, rowsShown).show();
                            $('#nav li a:first').addClass('active');
                            $('#nav a').bind('click', function () {
                                $('#nav a').removeClass('active');
                                $(this).addClass('active');
                                var currPage = $(this).attr('rel');
                                var startItem = currPage * rowsShown;
                                var endItem = parseInt(startItem, 10) + parseInt(rowsShown, 10);
                                $('#data tbody tr').css('opacity', '0.0').hide().slice(startItem,
                                    endItem).
                                css('display', 'table-row').animate({
                                    opacity: 1
                                }, 300);
                            });
                        });
                    </script>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>